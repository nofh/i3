# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# applications
set $terminal terminator
 
set $web1 iceweasel
set $web2 google-chrome
set $web3 uzbl

set $editeur1 xfwrite 
set $editeur2 gvim
set $editeur3 libreoffice

set $calculatrice gnome-calculator

set $repertoire xfe
set $repertoire_documents xfe ~/Documents
set $repertoire_dnet xfe ~/Téléchargements/DNet/
set $repertoire_www xfe /var/www

set $volume_mute  amixer set Master toggle
set $volume_plus  amixer set Master 5%+ unmute
set $volume_moins amixer set Master 5%- unmute

set $video1 totem
set $video2 mplayer

set $music1 musique
set $music2 audacious

set $memo ~/.i3/scripts/memo.sh
set $memo1 i3.memo
set $memo2 xfe.memo
set $memo3 terminator.memo
set $memo4 vim.memo
set $memo5 git.memo
set $memo6 i3.memo

# mod
set $mod Mod4

# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+q exec i3-sensible-terminal

# kill focused window
bindsym $mod+w kill
bindsym $mod+Shift+W xkill

# start dmenu (a program launcher)
bindsym $mod+e exec dmenu_run

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+ugrave split h

# split in vertical orientation
bindsym $mod+mu split v

# enter fullscreen mode for the focused container
bindsym $mod+equal fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+comma layout default
bindsym $mod+semicolon layout stacking
bindsym $mod+colon layout tabbed

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+dead_circumflex focus parent 

# focus the child container
bindsym $mod+dollar focus child

# switch to workspace
bindsym $mod+ampersand workspace 1
bindsym $mod+eacute workspace 2
bindsym $mod+quotedbl workspace 3
bindsym $mod+apostrophe workspace 4
bindsym $mod+parenleft workspace 5
bindsym $mod+section workspace 6
bindsym $mod+egrave workspace 7
bindsym $mod+exclam workspace 8:media
bindsym $mod+ccedilla workspace 9:www
bindsym $mod+agrave workspace 10:notif

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8:media
bindsym $mod+Shift+9 move container to workspace 9:www
bindsym $mod+Shift+0 move container to workspace 10:notif

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym l           resize shrink width 10 px or 10 ppt
        bindsym k           resize grow height 10 px or 10 ppt
        bindsym j           resize shrink height 10 px or 10 ppt
        bindsym h           resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left        resize shrink width 10 px or 10 ppt
        bindsym Down        resize grow height 10 px or 10 ppt
        bindsym Up          resize shrink height 10 px or 10 ppt
        bindsym Right       resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+x mode "resize"

# floating window size
floating_minimum_size 75 x 50
floating_maximum_size -1 x -1

# default workspace layout
workspace_layout stacking

# hiding vertical borders
#hide_edge_borders both # valide ds i3 4.2

# windows rules
for_window [class="XTerm"] floating enable
for_window [class="Calculatrice"] floating enable
for_window [class="Event Tester"] floating enable
for_window [class="Nautilus" instance="file_progress"] floating enable


# assign window on specific workspace
assign [class="^Osmo$"] 10:notif
assign [class="^Iceweasel$"] 9:www
assign [class="^mocp$"] 8:media
assign [class="^Totem$"] 8:media

# popup during fullscreen
# popup_during_fullscreen smart # fonctionne pas 

# no focus on parent container 
force_focus_wrapping yes

# move workspace to other monitor
bindsym $mod+Shift+n move workspace to output right
bindsym $mod+Shift+b move workspace to output left

# automatic starting app
exec --no-startup-id nm-applet
exec --no-startup-id osmo
exec --no-startup-id parcellite
exec --no-startup-id nitrogen --restore
exec --no-startup-id xrandr --output LVDS --brightness 0.8
# exec --no-startup-id 'workspace 9:notif; exec /usr/bin/urxvt'

# applications 
# a - t 
bindsym $mod+a exec $web1
bindsym $mod+Shift+A exec $web2
bindsym $mod+Shift+Control+A exec $web3

bindsym $mod+z exec $editeur1
bindsym $mod+Shift+Z exec $editeur2
bindsym $mod+Shift+Control+Z exec $editeur3

#bindsym $mod+e exec $editeur2 # dmenu

bindsym $mod+r exec $editeur2

bindsym $mod+t exec $editeur2

# y - p
bindsym $mod+p exec $web1
bindsym $mod+o exec $editeur1
bindsym $mod+i exec $editeur3
bindsym $mod+u exec $calculatrice
bindsym $mod+y exec $editeur1

# q - m
# q terminal
bindsym $mod+s exec $repertoire
bindsym $mod+d exec $repertoire_dnet
bindsym $mod+f exec $repertoire_www
bindsym $mod+g exec $repertoire_documents

bindsym $mod+m exec $music1
bindsym $mod+Shift+m exec $music2
#l
#k
#j
#h

# w -n
# w quit
# x resize
bindsym $mod+v exec $volume_mute
bindsym $mod+b exec $volume_moins
bindsym $mod+n exec $volume_plus

# memo
bindsym $mod+F1 exec $memo $memo1
bindsym $mod+F2 exec $memo $memo2
bindsym $mod+F3 exec $memo $memo3
bindsym $mod+F4 exec $memo $memo4
bindsym $mod+F5 exec $memo $memo5
bindsym $mod+F6 exec $memo $memo6
bindsym $mod+F7 exec $memo $memo7

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#       status_command i3status --config ~/.i3/i3status.conf 
#}


bar {
        position bottom
        status_command ~/.i3/scripts/conky
        colors { 
                statusline #AAAAAA
                background #333333
                focused_workspace #333333 #333333 #FFFFFF
                inactive_workspace #484848 #484848 #AAAAAA
        }
}
# vim:filetype=i3
